bookpages = int(input())
pages_per_hour = int(input())
days_for_reading = int(input())
hours_per_book = bookpages / pages_per_hour
hours_per_day = hours_per_book / days_for_reading
print(int(hours_per_day))
